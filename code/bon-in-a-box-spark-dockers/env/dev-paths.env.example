# dev-paths.env.example — config-stage path template
# Copy to .dev-paths.env inside bon-in-a-box-pipeline-engine checkout.
# The upstream engine expects these relative paths for local compose/dev mode.

PIPELINE_REPO_PATH=${PWD}/pipeline-repo
GATEWAY_CONFIG=./http-proxy/conf.d
