#!/bin/bash
set -euo pipefail

cd "$(dirname "$0")/.."

docker build -t naturedesk-jekyll-local .
docker run --rm -v "$PWD":/srv/jekyll -w /srv/jekyll naturedesk-jekyll-local sh -lc 'bundle exec jekyll build'
